import ShaderShowcase from "@/components/ui/hero";

/** Standalone demo route for the Paper Design shader hero (full-page showcase). */
export default function HeroDemo() {
  return (
    <div className="min-h-screen h-full w-full">
      <ShaderShowcase />
    </div>
  );
}
